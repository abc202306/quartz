filters:
  and:
    - up.containsAny(link("gallery-notes", "gallery-notes"))
views:
  - type: cards
    name: Cards
    order:
      - file.name
      - gallery
      - url
    sort:
      - property: ctime
        direction: DESC
    image: note.cover
    cardSize: 100
